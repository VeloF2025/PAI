# Installation - project-codebase

## Prerequisites
- Claude Code CLI installed

## Installation Steps
This is a knowledge-only skill with no dependencies.

Check files exist:
```bash
cd ~/.claude/skills/project-codebase
ls -la
```

No configuration or dependencies required.

---
**Format**: Pack v2.0
